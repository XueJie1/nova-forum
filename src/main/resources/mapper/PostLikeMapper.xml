<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.novaforum.nova_forum.mapper.PostLikeMapper">

    <!-- 结果映射 -->
    <resultMap id="PostLikeResultMap" type="com.novaforum.nova_forum.entity.PostLike">
        <id column="id" property="id"/>
        <result column="post_id" property="postId"/>
        <result column="user_id" property="userId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- 批量插入点赞记录 -->
    <insert id="insertBatch">
        INSERT INTO post_like (post_id, user_id, create_time) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.postId}, #{item.userId}, #{item.createTime})
        </foreach>
    </insert>

    <!-- 根据帖子ID删除所有点赞记录 -->
    <delete id="deleteByPostId">
        DELETE FROM post_like WHERE post_id = #{postId}
    </delete>

    <!-- 根据用户ID删除所有点赞记录 -->
    <delete id="deleteByUserId">
        DELETE FROM post_like WHERE user_id = #{userId}
    </delete>

</mapper>
