<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.novaforum.nova_forum.mapper.CommentMapper">

    <!-- 结果映射 -->
    <resultMap id="CommentResultMap" type="com.novaforum.nova_forum.entity.Comment">
        <id column="id" property="id"/>
        <result column="post_id" property="postId"/>
        <result column="user_id" property="userId"/>
        <result column="parent_id" property="parentId"/>
        <result column="content" property="content"/>
        <result column="create_time" property="createTime"/>
        <!-- 扩展字段 -->
        <result column="username" property="username"/>
        <result column="parent_content" property="parentContent"/>
        <result column="reply_count" property="replyCount"/>
    </resultMap>

    <!-- 根据帖子ID分页查询评论 -->
    <select id="selectCommentsByPostIdPage" resultMap="CommentResultMap">
        SELECT 
            c.*,
            u.username,
            p.content as parent_content,
            (SELECT COUNT(*) FROM comment WHERE parent_id = c.id) as reply_count
        FROM comment c
        LEFT JOIN user u ON c.user_id = u.id
        LEFT JOIN comment p ON c.parent_id = p.id
        <where>
            c.post_id = #{postId}
            <if test="parentId != null">
                AND c.parent_id = #{parentId}
            </if>
            <if test="parentId == null">
                AND c.parent_id IS NULL
            </if>
        </where>
        ORDER BY c.create_time ASC
        LIMIT #{page.size} OFFSET #{page.offset}
    </select>

    <!-- 查询帖子的所有评论（构建树形结构用） -->
    <select id="selectCommentsByPostId" resultMap="CommentResultMap">
        SELECT 
            c.*,
            u.username,
            p.content as parent_content,
            (SELECT COUNT(*) FROM comment WHERE parent_id = c.id) as reply_count
        FROM comment c
        LEFT JOIN user u ON c.user_id = u.id
        LEFT JOIN comment p ON c.parent_id = p.id
        WHERE c.post_id = #{postId}
        ORDER BY c.create_time ASC
    </select>

    <!-- 根据ID查询评论详情（包含用户信息） -->
    <select id="selectCommentWithUserById" resultMap="CommentResultMap">
        SELECT 
            c.*,
            u.username,
            p.content as parent_content,
            (SELECT COUNT(*) FROM comment WHERE parent_id = c.id) as reply_count
        FROM comment c
        LEFT JOIN user u ON c.user_id = u.id
        LEFT JOIN comment p ON c.parent_id = p.id
        WHERE c.id = #{id}
    </select>

    <!-- 查询评论的子评论数量 -->
    <select id="selectReplyCountByParentId" resultType="java.lang.Integer">
        SELECT COUNT(*) 
        FROM comment 
        WHERE parent_id = #{parentId}
    </select>

    <!-- 查询帖子的评论总数 -->
    <select id="selectCommentCountByPostId" resultType="java.lang.Integer">
        SELECT COUNT(*) 
        FROM comment 
        WHERE post_id = #{postId}
    </select>

    <!-- 查询用户的所有评论数量 -->
    <select id="selectCommentCountByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*) 
        FROM comment 
        WHERE user_id = #{userId}
    </select>

    <!-- 批量删除评论及其子评论 -->
    <delete id="deleteCommentsBatch">
        DELETE FROM comment 
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 根据帖子ID删除所有评论 -->
    <delete id="deleteCommentsByPostId">
        DELETE FROM comment 
        WHERE post_id = #{postId}
    </delete>

    <!-- 根据用户ID删除所有评论 -->
    <delete id="deleteCommentsByUserId">
        DELETE FROM comment 
        WHERE user_id = #{userId}
    </delete>

</mapper>
